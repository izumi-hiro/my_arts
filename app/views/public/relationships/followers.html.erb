<div class="container">
  <div class="row">
    <h3>フォロワー一覧</h3>
  </div>
  <div class="row">
    <% if @customers.present? %>
      <%= render 'public/customers/index', customers: @customers %>
    <% else %>
      <p>フォロワーはいません</p>
    <% end %>
  </div>
</div>
